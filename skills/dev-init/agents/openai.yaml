interface:
  display_name: "Dev Init"
  short_description: "Initialize Dev Flow project memory"
  default_prompt: "Use $dev-init to create the recommended Dev Flow memory structure for this project."
